function characterSequence(word) {

    for (let i = 0; i <= word.length - 1; i++) {

        let letter = word[i];
        console.log(letter);

    }
}
characterSequence("softuni");
characterSequence("ice cream");